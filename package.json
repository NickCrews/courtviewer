{
  "name": "alaska-court-viewer",
  "version": "1.1.0",
  "description": "Chrome extension for tracking Alaska court cases",
  "private": true,
  "scripts": {
    "build": "tsc && node build.js",
    "watch": "tsc --watch",
    "test": "npx playwright test",
    "clean": "rm -rf dist",
    "bundle": "npm run build && node build.js bundle",
    "publish-gh": "npm run bundle && gh release create v$(node -p \"require('./package.json').version\") release/alaska-court-viewer-$(node -p \"require('./package.json').version\").zip --draft"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@playwright/test": "^1.58.0",
    "@types/chrome": "^0.0.260",
    "archiver": "^6.0.0"
  }
}
